Integrantes : Marcos Ariel Vazquez , Ignacio Gomez Spindola, Emiliano Veron ,Crstopher Alexander Quiñonez.

Decisiones de diseño toomadas: se tomo la desicion en conjuto de resolver el trabajo practico dandole una estructura MVC.

Descripcion : Este proyecto cuenta con: 6 paquetes y 4 archivos de texto (txt).

Paquete.InterfazGrafica:

	1) VentanaInicio.java
	    Este archivo es una interfaz grafica que da la bienvenida al ATM y al mismo tiempo carga los archivos de la base de datos(en este caso 
	    de los archivos txt).
	  
	2) VentanaValidacionDeTarjeta.java
	    Este archivo es una interfaz grafica el cual recive: un numero de tarjeta y su pin, con el cual se verifica su existencia en la base de
	    datos.Si el pin y el numero de tarjeta son   correctos podra acceder a SeleccionarCuenta.java de lo contrario si el pin es incorrecto
	    se abrira el archivo DatosIncorrectos.java , tras 3 intentos fallidos se abrira Bloqueo DeTarjeta.java .
	    En el caso de que el numero de tarjeta no exista en la base de datos se le comunicara al usuario mediante una ventana.
	  
    3) DatosIncorrectos.java
	    Este archivo es una interfaz grafica que unicamente se ejecuta cuando el pin ingresado, correspondiente al numero de tarjeta ingresado,
	    es incorrecto.
	   
	4) BloqueoDeTarjeta.java
		Este archivo es una interfaz grafica que  unicamente se ejecuta cuando el pin de la tarjeta ingresada
		es incorrecto en 3 oportunidades consecutivas.Para volver a utilizar el ATM es necesario Reiniciarlo.
		
	5) SeleccionarCuenta.java
	    Este archivo es una interfaz grafica que se ejecuta luego de verificar que el numero de tarjeta y pin son correctos.Esta interfaz
	    permite al usuario elegir con cual tipo de cuenta desea operar.Ademas podra consultar sus ultimos movimientos y tambien podra volver a
	    VentanaValidacionDeTarjeta.java para ingresar otra tarjeta diferente.
	    
	6) CajaDeAhorroEnPesos.java
		Este archivo es una interfaz grafica en la cual se pueden elegir distintas operaciones correspondientes a una cuanta de tipo
		caja de ahorro en pesos .Retirar dinero ,depositar, comprar dolares,transferir dinero ,obtener saldo y eliminar transferencia.
	
	7) CajaDeAhorroEnDolares.java
		Este archivo es una interfaz grafica en la cual se pueden elegir distintas operaciones correspondientes a una cuanta de tipo
		caja de ahorro en dolares .En este caso depositar  y obtener saldo.
		
	8) CuentaCorriente.java
		Este archivo es una interfaz grafica en la cual se pueden elegir distintas operaciones correspondientes a una cuanta de tipo
		cuenta corriente .Retirar dinero ,depositar, comprar dolares,transferir dinero ,obtener saldo y eliminar transferencia.
		
	9) Transferir.java
	    Este archivo es una interfaz grafica en la cual se realizan transferencias mediante un alias y un monto. En caso de que 
	    el monto o el alias no sea especificado saldra una ventana que especifica la informacion necesaria,  para poder  realizar
	    la operacion.Unicamente se pueden transrefir entre cuentas del mismo tipo y si el saldo no es infuficiente.
	    
	10) RanuraDeDeposito.java
	     Este archivo es una interfaz grafica la cual recive un monto ,estipulado por el usuario, que sera depositado en el tipo 
	     de cuenta seleccionada en SeleccionarCuenta.java.
	  
	11) ComprarDolares.java
	     Este archivo es una interfaz grafica en donde se le especifica un monto en dolares a comprar ,si el saldo es suficiente 
	     se realizara la compra y se la entrega en efectivo. 
	     NOTA: EL PRECIO DEL DOLAR ($120) ES FIJO Y NO SE PUEDE MODIFICAR Y ADEMAS SE LE SUMA EL IMPUESTO PAIS A LA COMPRA DE DOLARES.
	
	12) RetirarEfectivo.java
	     Este archivo es una interfaz grafica en donde unicamente se puede retirar multiplos del los montos que se muetrasn en pantalla.
	     Es decir que el usuario no puede especificar un monto cualquiera,si el saldo es suficiente se realiza la operacion.	
		

Paquete.CodigoFuente

	1) Banco.java
	    Este  archivo contiene todas las operaciones posibles que se pueden realizar con el ATM.
	    
	2) Cuenta.java
	     Este archivo :guarda los datos de una Cuenta ,verifica que los datos sean correctos, devuelve los datos ,modifica y actualiza
	     el saldo de las cuentas;
	     
	3) CuentaDeAhorroEnPesos.java
	    Este archivo extiende a Cuenta.
	 
	4) CuentaDeAhorroEnDolares.java
	    Este Archivo extiende a Cuenta.
	    
	5) CuentaCorriente.java
	    Este archivo extiende a Cuenta.
	    
	6) Cliente.java
	    Este archivo guarda los valores del cliente y los devuelve.Ademas posee un metodo para asignarle una tarjeta.
	    
	7) Billete.java
	    Este archivo : guarda un valor de un billete , devuelve el valor del Billete y devuelve la  cantidad 
	    de billetes restantes .Es Utilizado para el dispencerDeEfectivo.java.
	    
	8) DispencerDeEfectivo.java
	    Este archivo esta relacionado con RetirarEfectivo.java , este archivo contiene la logica para poder retirar efectivo.
	
	9) Tarjeta.java
	    Este archivo guarda los valores de una tarjeta y los devuelve.Tambien tiene un metodo para asignarle una Cuenta de cualquier tipo.
	  
	10) Transferencia.java
	     Este archivo guarda los valores de una transferencia y los devuelve.
	     
	11) UltimosMovimientos.java
		 Este archivo guarda y devuelve los ultimos movimientos del usuario .
	
Paquete.Lectores:
	Contiene 3 archivos.java los cuales se encargan de leer los archivos de texto para constuir la base de datos .
	
	
Paquete.PruebasUnitarias:
	Contiene todas las pruebas que verifican el correcto funcionamiento del programa.
	
	
Paquete.Imagenes:
	Contiene todas las imagenes que utiliza el programa.
	
	
Paquete.Exceptions:
	Contiene todas las excepciones que podria lanzar el programa durante su ejecucion.
	
Conclusiones:En este trabajo se busca que un usuario comun y corriente pueda utilizar dicha interfaz sin ningun tipo de problema,
de manera  tal  que pueda acceder a todas las caracteristicas que este programa le propone . Ya sea para comprar dolares ,depositar fondos,etc..
para que de esta forma le resulte algo comodo y facil de utilizar.